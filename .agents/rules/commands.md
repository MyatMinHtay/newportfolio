# Terminal Commands Policy

Run safe terminal commands automatically without asking. Always ask for confirmation before running any command that modifies or drops databases, resets migrations, or deletes files (e.g., migrate:fresh, db:wipe, drop, rm -rf).
